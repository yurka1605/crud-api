export const defaultHost = 'localhost';
export const defaultPort = 4000;
export const urlIdentifier = ':id';
export const clusterFlag = '--multi';
export const serverErrorMessage = `
  An error occurred while processing the request, 
  contact the administrator or check if your request is correct
`;
